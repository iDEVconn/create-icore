# Progress Ledger — PR1: role-jwt-refresh-contract

Branch: bug/postgres-role-jwt-refresh-contract (worktree: .claude/worktrees/bug+postgres-role-jwt-refresh-contract)
Base: dev @ b0ee749
Plan: docs/superpowers/plans/2026-07-14-pr1-role-jwt-refresh-contract.md

(Prior ledger was for feature/leftovers — merged via PR #232, dev @ b0ee749. Cleared to start this plan. Note: this ledger was accidentally written to the main checkout path instead of the worktree for a few steps early on — recreated here at the correct path; no task state was lost since Task 1 hadn't been marked complete yet.)

## Tasks
- [x] Task 1: Auth MS re-mints the session after role assignment
- [x] Task 2: Client API layer matches the gateway's camelCase refresh contract
- [x] Task 3: Changeset + build gate

Task 2: complete (commits d0eec88..a151cdf, review clean, 0 findings). Implementer also added 'vitest' to template-shared's eslint.config.mjs ignoredDependencies — necessary since this was the lib's first test file (@nx/dependency-checks flags it otherwise); reviewer confirmed this matches the same fix already present in storage-strategies/auth-strategies/db-strategies libs.
Task 3: complete (commits a151cdf..95982e8, review clean, 0 findings). Changeset-only commit, build gate green (11/11 tasks).

ALL 3 TASKS COMPLETE. Final whole-branch review (opus): Ready to merge, 0 Critical/Important, 3 Minor (noted eslint ignoredDependencies deviation, last_logged_in-on-signup side effect, coverage-gap-by-design for magic-link/oauth) — none blocking.

DONE. Branch bug/postgres-role-jwt-refresh-contract ready for PR --base dev.
PR #238 opened: https://github.com/iDEVconn/create-icore/pull/238. CI running as of push. Waiting for green — no autonomous merge (AGENTS.md).

CI run 29351468114 failed 7/7 "Scaffold smoke" matrix jobs. Root cause (systematic-debugging): Task 1's new apps/microservices/auth/src/app/__tests__/auth.controller.postgres.integration.unit.test.ts imports @icore/auth-postgres unconditionally, but the auth manifest's postgres unit (tools/create-icore/src/manifest/index.ts) had no appTests entry (unlike supabase/firebase) — cleanupUnusedAuth() never pruned it for non-postgres scaffolds, so the audit flagged an orphan import the moment the lib was removed. Fixed in commit cd58d76: added the appTests entry + extended wire-auth.unit.test.ts's fixture/assertions to cover postgres. Verified via unit test (171/171 pass) AND by reproducing the exact failing CI command locally (node tools/create-icore/scripts/smoke-scaffold.mjs --auth=supabase ...) — now passes; also verified --auth=postgres still keeps the file. Pushed, watching new CI run.

Task 1: complete (commits 1205662..3bd1655, review clean, 0 findings). Note: plan's file paths were stale (prefixed `tools/create-icore/templates/`, which is a gitignored build artifact regenerated by snapshot-templates.mjs — real source is repo-root `apps/`/`libs/`). Implementer used the correct real path on their own; reviewer confirmed. Plan doc fixed in commit d0eec88 (the whole plan set had the same stale prefix — PR2-5 plan docs fixed in the main checkout too, not yet committed since those branches don't exist yet).
