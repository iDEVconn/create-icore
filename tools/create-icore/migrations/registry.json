{
  "entries": [
    {
      "id": "payment-never-crash-factory",
      "kind": "codemod",
      "affectedAxes": [
        "payment:paypal"
      ],
      "affectedGlobs": [
        "apps/microservices/payment/src/app/app.module.ts",
        "apps/microservices/payment/src/app/fake-payment.strategy.ts"
      ],
      "commitRange": "ddfa6d1d9fe43db0178c074f0d6258efb34716d4..c13711e6c3ce239df97a4f38d219636c571aa84f",
      "description": "Payment MS's PaymentRegistry factory no longer throws on boot in production when PayPal credentials are missing. Patches app.module.ts to always register a strategy for the configured provider (real PaypalStrategy when creds exist, FakePaymentStrategy otherwise) and ships the FakePaymentStrategy fallback file.",
      "version": "0.14.0",
      "diff": "diff --git a/apps/microservices/payment/src/app/app.module.ts b/apps/microservices/payment/src/app/app.module.ts\nindex a4259f0..b51c37e 100644\n--- a/apps/microservices/payment/src/app/app.module.ts\n+++ b/apps/microservices/payment/src/app/app.module.ts\n@@ -1,9 +1,15 @@\n import { join } from 'node:path';\n import { Module, Logger } from '@nestjs/common';\n import { ConfigModule, ConfigService } from '@nestjs/config';\n-import { PaymentRegistry, PaypalStrategy, createPayment } from '@idevconn/payment';\n+import {\n+  PaymentRegistry,\n+  PaypalStrategy,\n+  createPayment,\n+  type PaymentStrategy,\n+} from '@idevconn/payment';\n import { missingEnv, formatEnvBanner } from '@icore/shared';\n import { PaymentController } from './payment.controller';\n+import { FakePaymentStrategy } from './fake-payment.strategy';\n \n const ENV_PATH = 'apps/microservices/payment/.env';\n \n@@ -32,31 +38,28 @@ const REQUIRED_ENV: Record<string, string[]> = {\n         if (!keys) throw new Error(`Unsupported PAYMENT_PROVIDER: ${provider}`);\n \n         const missing = missingEnv((k) => cfg.get<string>(k), keys);\n-        if (missing.length > 0) {\n-          const banner = formatEnvBanner({\n-            service: 'payment MS',\n-            provider,\n-            missing,\n-            envPath: ENV_PATH,\n-            headline: `⚠  payment MS — ${provider} credentials missing (payments will fail)`,\n+\n+        const buildStrategy = (): PaymentStrategy => {\n+          if (missing.length > 0) {\n+            const banner = formatEnvBanner({\n+              service: 'payment MS',\n+              provider,\n+              missing,\n+              envPath: ENV_PATH,\n+              headline: `⚠  payment MS — ${provider} credentials missing (payments will fail)`,\n+            });\n+            logger.warn(banner);\n+            return new FakePaymentStrategy(provider);\n+          }\n+\n+          return new PaypalStrategy({\n+            clientId: cfg.getOrThrow<string>('PAYPAL_CLIENT_ID'),\n+            secret: cfg.getOrThrow<string>('PAYPAL_CLIENT_SECRET'),\n+            environment: cfg.get<'sandbox' | 'live'>('PAYPAL_ENVIRONMENT') ?? 'sandbox',\n           });\n-          // Prod: fail fast. Dev: warn + register an EMPTY strategy map so the\n-          // MS boots (PaypalStrategy's constructor throws on blank creds, so we\n-          // must not instantiate it) — payment endpoints fail until creds are set.\n-          if (process.env.NODE_ENV === 'production') throw new Error(banner);\n-          logger.warn(banner);\n-          return createPayment({ strategies: {} });\n-        }\n+        };\n \n-        return createPayment({\n-          strategies: {\n-            paypal: new PaypalStrategy({\n-              clientId: cfg.getOrThrow<string>('PAYPAL_CLIENT_ID'),\n-              secret: cfg.getOrThrow<string>('PAYPAL_CLIENT_SECRET'),\n-              environment: cfg.get<'sandbox' | 'live'>('PAYPAL_ENVIRONMENT') ?? 'sandbox',\n-            }),\n-          },\n-        });\n+        return createPayment({ strategies: { [provider]: buildStrategy() } });\n       },\n       inject: [ConfigService],\n     },\ndiff --git a/apps/microservices/payment/src/app/fake-payment.strategy.ts b/apps/microservices/payment/src/app/fake-payment.strategy.ts\nnew file mode 100644\nindex 0000000..4c5bbd4\n--- /dev/null\n+++ b/apps/microservices/payment/src/app/fake-payment.strategy.ts\n@@ -0,0 +1,28 @@\n+import {\n+  PaymentError,\n+  type CaptureResult,\n+  type CreateOrderInput,\n+  type OrderResult,\n+  type PaymentStrategy,\n+  type RequestOptions,\n+} from '@idevconn/payment';\n+\n+export class FakePaymentStrategy implements PaymentStrategy {\n+  constructor(readonly name: string) {}\n+\n+  createOrder(_input: CreateOrderInput, _options?: RequestOptions): Promise<OrderResult> {\n+    return Promise.reject(this.notConfigured());\n+  }\n+\n+  captureOrder(_orderId: string, _options?: RequestOptions): Promise<CaptureResult> {\n+    return Promise.reject(this.notConfigured());\n+  }\n+\n+  private notConfigured(): PaymentError {\n+    return new PaymentError(\n+      'PROVIDER_ERROR',\n+      503,\n+      `Payment provider \"${this.name}\" is not configured (missing credentials)`,\n+    );\n+  }\n+}\n"
    }
  ]
}
