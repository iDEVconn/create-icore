# db-firestore

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build db-firestore` to build the library.

## Running unit tests

Run `nx test db-firestore` to execute the unit tests via [Vitest](https://vitest.dev/).
